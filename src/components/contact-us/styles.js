import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(()=> ({
  contactusform: {
    display: "flex",
    flexDirection: "column",
  },
  
  title: {
    marginTop:10,
    marginBottom:10,
  },
}))